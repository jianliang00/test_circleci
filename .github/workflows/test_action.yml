name: Commit Queue Workflow

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: jianliang00/commit-queue-actions@fdff17bcb4a723592940f1bae711f6bd029c66e1
      
      # Add your steps here
      - name: Build
        run: echo "Your commands here"
